<!DOCTYPE html>
<html>
  <head>
    <title>Online Sales Tracker</title>
    <link rel="stylesheet" href="/stylesheets/pages.css"/>
  </head>
  <body>
    <nav>
      <button class="bar"><a href="/sales" class="plain">View My Sales</a></button>
      <button class="bar"><a href="/sales/new" class="plain">Add a New Sale</a></button>
      <button class="logout bar"><a href="/logout" class="plain">Logout</a></button>
    </nav>

    <h1 class="head">Hello, <%= @user.name %></h1>
    <h2 class="subhead">Here's a snapshot of the sales that have been made recently:</h2>
    <% @sales.sort_by(&:created_at).reverse.each do |sale| %>
      <h3><%= sale.user.name%> made $<%= sale.amount.to_i %> on <%= sale.created_at.strftime("%B %m, %Y") %></h3><br>
    <% end %>
  </body>
</html>
