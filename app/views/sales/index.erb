<!DOCTYPE html>
<html>
  <head>
    <title>Online Sales Tracker</title>
    <link rel="stylesheet" href="/stylesheets/pages.css"/>
  </head>
  <body>
    <nav>
      <button class="bar"><a href="/" class="plain">Profile</a></button>
      <button class="bar"><a href="/sales/new" class="plain">Add a New Sale</a></button>
      <button class="logout bar"><a href="/logout" class="plain">Logout</a></button>
    </nav>

    <%# <h1 class="head">Current total revenue: <%= total_revenue() %> 

    <h1>Here are your sales:</h1>
    <img src="images/sale.png"> 

    <% @sales.each do |sale| %>

      <% if authorized_edit?(sale) %>
        <section>
          <a href="/sales/<%= sale.id %>" class="sale"><h2><%= sale.title %>: $<%= sale.amount %><h2></a>
        </section>
      <% end%>
      
    <% end %>

  </body>
</html>










