<!DOCTYPE html>
<html>
  <head>
    <title>Online Sales Tracker</title>
    <link rel="stylesheet" href="/stylesheets/pages.css"/>
  </head>
  <body>
    <nav>
      <button class="bar"><a href="/" class="plain">Profile</a></button>
      <button class="bar"><a href="/sales" class="plain">View My Sales</a></button>
      <button class="logout bar"><a href="/logout" class="plain">Logout</a></button>
    </nav>

    <h1 class="head"><%= @sale.title %></h1>

    <section>
      <h2>Amount: $<%= sprintf '%.2f', @sale.amount %></h2>
      <h2>Date: <%= @sale.created_at.strftime("%B %d, %Y") %></h2>
      <button class="enter"><a href="/sales/<%= @sale.id %>/edit" class="plain">Edit this sale</a></button><br><br>
      <form action="/sales/<%= @sale.id %>" method="post">
        <input type="hidden" id="hidden" name="_method" value="delete">
        <button type="submit" class="enter">Delete</button>
      </form>
    </section>
  </body>
</html>